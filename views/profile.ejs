<!DOCTYPE html>
<html>
<head>
  <title><%= profileuser%>'s Profile</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <h1>Welcome, <%= profileuser %>!</h1>

  <div class="profile-card">
    <% if (user.avatarUrl) { %>
      <img src="<%= user.avatarUrl %>" alt="Profile Picture" width="150">
    <% } else { %>
      <img src="/images/default-avatar.png" alt="Default Avatar" width="150">
    <% } %>

    <p><strong>Email:</strong> <%= user.email %></p>
    <p><strong>Bio:</strong> <%= user.bio || 'No bio yet.' %></p>

    <a href="/matches">View Matches</a> | 
    <a href="/likes">View Likes</a>
  </div>
</body>
</html>